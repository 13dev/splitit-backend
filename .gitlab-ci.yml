image: tmaier/docker-compose:latest

services:
  - docker:dind

before_script:
  - docker info
  - docker-compose --version
  - sudo chown $USER:$USER -R ~/builds/

after_script:
  - sudo chown $USER:$USER -R ~/builds/

cache:
  untracked: true
  paths:
    - vendor/

stages:
  - build
  - test
  - deploy

build:
  stage: build
  only:
    - dev
    - master
  script:
    - sudo docker image prune -f
    - sudo docker-compose build --no-cache
    - sudo docker-compose up -d

test:
  stage: test
  only:
    - dev
    - master
  script:
    - echo run tests in this section

step-deploy-prod:
  stage: deploy
  only:
    - master
  script:
    - docker-compose exec -it php composer install
    - docker-compose exec -it php cp .env.example .env
    - docker-compose exec -it php php artisan key:generate
    - docker-compose exec -it php php artisan config:cache
    - docker-compose exec -it php php artisan db:create -v
    - docker-compose exec -it php php artisan migrate -v
    - docker-compose exec -it php php artisan db:seed
    #- sudo chown $USER:$USER -R .

  environment: production
  when: manual
