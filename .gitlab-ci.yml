image: docker

services:
  - docker:dind

stages:
  - test
  - deploy

test:
  stage: test
  only:
    - dev
    - master
  script:
    - echo run tests in this section

step-deploy-prod:
  stage: deploy
  only:
    - master
  script:
    - sudo apt-get install -y python3-pip
    - sudo pip3 install --upgrade setuptools
    - sudo pip3 install docker-compose
    - sudo docker image prune -f
    - echo "$USER"
    - sudo docker-compose build
    - sudo docker-compose up -d
    - sudo docker-compose run php composer install
    - sudo docker-compose run php cp .env.example .env
    - sudo docker-compose run php php artisan key:generate
    - sudo docker-compose run php php artisan config:cache
    - sudo docker-compose run php php artisan db:create
    - sudo docker-compose run php php artisan migrate
    - sudo docker-compose run php php artisan db:seed
  environment: production
  when: manual
